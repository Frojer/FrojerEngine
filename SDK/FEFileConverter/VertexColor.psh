
////////////////////////////////////////////////////////////////////////////// 
//
// Pixel Shader Main
//
float4 PS_Main(
				float4 pos : SV_POSITION, //[입력] 정점좌표. (Screen, 2D)
				float4 col : COLOR0 //[입력] 정점색. (Vertex Color : "Diffuse")
				) : SV_TARGET
{
	//float4 col2 = {1, 0, 1, 1};
	 
    return col;
}